import React from 'react';

export const metadata = {
  title: 'Changelog | Aluminium ERP',
};

export default function ChangelogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 