import React from 'react';

export const metadata = {
  title: 'Help Center | Aluminium ERP',
};

export default function HelpLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 